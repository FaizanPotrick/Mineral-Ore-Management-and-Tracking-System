{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Here make sure to first install truffle globally using\r ``` npm i truffle -g ```",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "truffle-hdwallet-provider": "^1.0.17"
  }
}
